// Stub for the `server-only` package under vitest. See vitest.config.ts.
// The real package throws on import to keep server modules out of client
// bundles; that guarantee is enforced by the Next build, not by the tests.
export {}
